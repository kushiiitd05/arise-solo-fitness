# Phase 09 — Sub-plan 01: Fix `stats_bonus` → `effects` Interface

**Goal:** Normalise the TypeScript interface so it matches the DB column name `effects`,
eliminating the mismatch that currently makes stat bonuses invisible.

---

## Tasks

### T1 — Update `UserItem` interface in `inventoryService.ts`

**File:** `src/lib/services/inventoryService.ts`

Change the nested `items` shape inside `UserItem`:

```diff
-  stats_bonus: any;
+  effects: Record<string, number> | null;
```

Full updated interface:
```ts
export interface UserItem {
  id: string;
  user_id: string;
  item_id: string;
  equipped: boolean;
  quantity: number;
  acquired_at: string;
  items?: {
    name: string;
    description: string;
    rarity: string;
    type: string;
    effects: Record<string, number> | null;
    image_url: string | null;
  }
}
```

No logic changes needed — the DB already uses `effects`; this fixes the TS type only.

---

### T2 — Update `Inventory.tsx` references from `stats_bonus` → `effects`

**File:** `src/components/arise/Inventory.tsx`

Two occurrences to fix (lines ~189 and ~195):

```diff
-  {selectedItem.items?.stats_bonus && Object.entries(selectedItem.items.stats_bonus).map(([stat, val]: any) => (
+  {selectedItem.items?.effects && Object.entries(selectedItem.items.effects).map(([stat, val]) => (
     <div key={stat} className="flex justify-between items-center border-b border-white/[0.02] pb-1.5">
       <span className="text-[10px] font-black text-slate-400 uppercase tracking-widest">{stat}</span>
-      <span className="text-sm font-title font-black text-[#7C3AED] font-system">+{val}</span>
+      <span className="text-sm font-title font-black text-[#7C3AED] font-system">+{val as number}</span>
     </div>
   ))}
-  {!selectedItem.items?.stats_bonus && (
+  {!selectedItem.items?.effects && (
     <div className="text-[10px] font-black text-slate-600 uppercase tracking-[0.2em] italic">
       NO_MODIFIERS_DETECTED
     </div>
   )}
```

---

## Acceptance Criteria
- `UserItem.items.effects` typed as `Record<string, number> | null`
- `stats_bonus` no longer appears anywhere in the codebase (verify with grep)
- `Inventory.tsx` renders item stat bonuses by reading `item.items.effects`
- TypeScript compiles without errors on these files
